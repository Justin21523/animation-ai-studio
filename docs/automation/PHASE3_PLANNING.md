# Phase 3: Data Processing Automation - 規劃文檔

**開始日期**: 2025-12-02
**預計完成**: 2025-12-16 (2 週)
**狀態**: 🚀 Started

---

## 目標

建立完整的資料處理自動化工具集，涵蓋資料集建構、標註格式轉換和資料增強，為 AI 模型訓練提供高品質資料準備工具。

## 核心原則

- ✅ **CPU-only 處理** - 零 GPU 使用
- ✅ **記憶體安全** - 整合 Phase 1 安全基礎設施
- ✅ **生產品質** - 完整測試和文檔
- ✅ **批量處理** - 支援 YAML 配置的批量工作流程
- ✅ **模組化設計** - 每個工具獨立運作，可組合使用

---

## 組件 1: Dataset Builder（資料集建構器）

### 目標
提供全面的資料集建構和管理工具，支援多種資料集格式和工作流程。

### 核心功能

#### 1.1 資料集創建
- **從目錄結構創建**
  - ImageFolder 格式（類別名稱作為資料夾）
  - 自動掃描和索引
  - 支援遞迴搜尋

- **從現有資料集轉換**
  - 匯入 COCO, YOLO, Pascal VOC 格式
  - 保留標註資訊
  - 格式標準化

- **從檔案清單創建**
  - CSV/JSON 檔案清單
  - 支援自訂欄位映射
  - 靈活的來源路徑處理

#### 1.2 資料集分割
- **Train/Val/Test 分割**
  - 可配置比例（如 70/20/10）
  - 隨機分割（可設定種子）
  - 分層分割（保持類別比例）
  - 基於檔案名模式分割

- **K-fold 交叉驗證分割**
  - 生成 K 個 fold
  - 支援分層抽樣

#### 1.3 元數據生成
- **資料集摘要**
  - 總檔案數、類別數
  - 檔案大小統計
  - 分割統計（train/val/test 數量）

- **類別統計**
  - 每個類別的樣本數
  - 類別分佈視覺化
  - 類別不平衡檢測

- **檔案統計**
  - 圖像尺寸分佈
  - 格式分佈
  - 檔案大小分佈

#### 1.4 品質驗證
- **完整性檢查**
  - 檢測損壞的圖像檔案
  - 檢測缺失的標註檔案
  - 檢測空白或極小的圖像

- **標註驗證**
  - 檢查標註格式正確性
  - 檢查邊界框越界
  - 檢查類別 ID 有效性

- **品質過濾**
  - 移除損壞檔案
  - 移除低品質圖像
  - 生成品質報告

#### 1.5 資料集操作
- **合併資料集**
  - 合併多個資料集
  - 處理類別衝突
  - 自動重新編號

- **子集提取**
  - 按類別提取
  - 按數量提取
  - 按條件過濾

- **資料集轉換**
  - 格式轉換（COCO ↔ YOLO ↔ ImageFolder）
  - 路徑重新映射
  - 結構重組

### 命令列介面

```bash
# 從目錄創建資料集
python dataset_builder.py create-from-dir \
  --input-dir /path/to/images \
  --output-dir /path/to/dataset \
  --format imagefolder \
  --split-ratio 0.7,0.2,0.1 \
  --stratify

# 驗證資料集品質
python dataset_builder.py validate \
  --dataset-dir /path/to/dataset \
  --check-images \
  --check-annotations \
  --output-report /path/to/report.json

# 生成資料集統計
python dataset_builder.py stats \
  --dataset-dir /path/to/dataset \
  --output-dir /path/to/stats \
  --visualize

# 合併資料集
python dataset_builder.py merge \
  --input-datasets /path/to/dataset1 /path/to/dataset2 \
  --output-dir /path/to/merged \
  --handle-conflicts rename

# 提取子集
python dataset_builder.py extract-subset \
  --dataset-dir /path/to/dataset \
  --output-dir /path/to/subset \
  --classes cat dog \
  --max-samples 1000
```

### 配置檔案範例

```yaml
# dataset_builder_config.yaml
dataset_builder:
  # 資料集創建
  create:
    input_dir: /path/to/raw/images
    output_dir: /path/to/dataset
    format: imagefolder

    # 分割配置
    split:
      method: stratified  # random, stratified, or pattern
      ratio: [0.7, 0.2, 0.1]  # train, val, test
      seed: 42

    # 品質控制
    quality:
      min_image_size: [32, 32]
      max_image_size: [4096, 4096]
      check_corruption: true
      remove_duplicates: true

  # 驗證配置
  validate:
    check_images: true
    check_annotations: true
    check_class_balance: true
    warnings:
      class_imbalance_threshold: 0.1  # 10% 差異觸發警告
      min_samples_per_class: 10
```

### 輸出結構

```
output_dir/
├── train/
│   ├── class1/
│   │   ├── image1.jpg
│   │   ├── image2.jpg
│   │   └── ...
│   ├── class2/
│   └── ...
├── val/
│   └── ...
├── test/
│   └── ...
├── metadata.json          # 資料集元數據
├── statistics.json        # 統計資訊
├── quality_report.json    # 品質報告
└── dataset_info.txt       # 人類可讀的摘要
```

### 預計工作量
- **實施**: 2-3 天
- **測試**: 1 天
- **文檔**: 1 天
- **總計**: 4-5 天

---

## 組件 2: Annotation Tool Integration（標註工具整合）

### 目標
提供標註格式轉換和處理工具，支援主流標註工具和格式。

### 核心功能

#### 2.1 格式轉換
- **支援的格式**
  - COCO (JSON)
  - YOLO (txt)
  - Pascal VOC (XML)
  - LabelMe (JSON)
  - TFRecord
  - Custom JSON

- **轉換方向**
  - COCO → YOLO, Pascal VOC, LabelMe
  - YOLO → COCO, Pascal VOC
  - Pascal VOC → COCO, YOLO
  - 雙向轉換支援

#### 2.2 標註處理
- **批量轉換**
  - 遍歷整個資料集
  - 保留目錄結構
  - 進度追蹤

- **標註驗證**
  - 格式檢查
  - 座標合法性檢查
  - 類別 ID 驗證

- **標註修復**
  - 修正越界邊界框
  - 移除無效標註
  - 標準化類別名稱

#### 2.3 標註統計
- **標註摘要**
  - 總標註數量
  - 每個類別的標註數
  - 每張圖像的平均標註數

- **標註分佈**
  - 邊界框尺寸分佈
  - 位置分佈（熱力圖）
  - 長寬比分佈

#### 2.4 標註視覺化
- **邊界框視覺化**
  - 在圖像上繪製邊界框
  - 顯示類別標籤
  - 顏色編碼

- **批量視覺化**
  - 生成視覺化網格
  - 隨機抽樣展示
  - 輸出為圖像或 HTML

### 命令列介面

```bash
# COCO 轉 YOLO
python annotation_converter.py convert \
  --input-format coco \
  --output-format yolo \
  --input-file /path/to/annotations.json \
  --output-dir /path/to/yolo_annotations \
  --class-names-file /path/to/classes.txt

# 驗證標註
python annotation_converter.py validate \
  --format coco \
  --annotation-file /path/to/annotations.json \
  --images-dir /path/to/images \
  --output-report /path/to/report.json

# 視覺化標註
python annotation_converter.py visualize \
  --format yolo \
  --annotations-dir /path/to/yolo_annotations \
  --images-dir /path/to/images \
  --output-dir /path/to/visualizations \
  --num-samples 50
```

### 預計工作量
- **實施**: 2-3 天
- **測試**: 1 天
- **文檔**: 1 天
- **總計**: 4-5 天

---

## 組件 3: Data Augmentation Pipeline（資料增強管道）

### 目標
提供 CPU-only 的資料增強工具，支援管道式增強和批量處理。

### 核心功能

#### 3.1 幾何變換
- **基礎變換**
  - 水平/垂直翻轉
  - 旋轉（任意角度）
  - 縮放（放大/縮小）
  - 平移
  - 裁切（隨機/中心）

- **進階變換**
  - 透視變換
  - 仿射變換
  - 彈性變形
  - 網格扭曲

#### 3.2 顏色增強
- **亮度調整**
  - 增加/減少亮度
  - 自適應亮度

- **對比度調整**
  - 線性對比度
  - 直方圖均衡化
  - CLAHE（對比度限制自適應直方圖均衡化）

- **顏色空間調整**
  - 飽和度調整
  - 色調偏移
  - RGB 通道偏移

#### 3.3 雜訊和濾鏡
- **雜訊添加**
  - 高斯雜訊
  - 椒鹽雜訊
  - 泊松雜訊
  - 乘性雜訊

- **濾鏡效果**
  - 高斯模糊
  - 運動模糊
  - 銳化
  - 邊緣增強

#### 3.4 進階增強
- **MixUp**
  - 混合兩張圖像
  - 可配置混合比例

- **CutMix**
  - 裁切並混合圖像區域
  - 標籤按面積比例混合

- **Mosaic**
  - 拼接 4 張圖像
  - 常用於物體偵測訓練

#### 3.5 管道式增強
- **增強管道**
  - 組合多個增強操作
  - 可配置執行機率
  - 順序執行或隨機選擇

- **增強策略**
  - AutoAugment
  - RandAugment
  - TrivialAugment
  - 自訂策略

### 命令列介面

```bash
# 單一增強操作
python data_augmentation.py augment \
  --input-dir /path/to/images \
  --output-dir /path/to/augmented \
  --operation rotate \
  --params angle=45 \
  --num-augmented 3

# 管道式增強
python data_augmentation.py pipeline \
  --input-dir /path/to/images \
  --output-dir /path/to/augmented \
  --config /path/to/augmentation_config.yaml \
  --num-augmented 5

# 批量增強（從配置檔案）
python data_augmentation.py batch \
  --config /path/to/batch_config.yaml
```

### 配置檔案範例

```yaml
# augmentation_config.yaml
augmentation_pipeline:
  # 增強策略
  strategy: sequential  # sequential, random_choice, or probability

  # 增強操作列表
  operations:
    - name: horizontal_flip
      probability: 0.5

    - name: rotate
      probability: 0.3
      params:
        angle_range: [-15, 15]

    - name: brightness
      probability: 0.4
      params:
        factor_range: [0.8, 1.2]

    - name: gaussian_blur
      probability: 0.2
      params:
        kernel_size: [3, 7]

  # 輸出配置
  output:
    num_augmented_per_image: 5
    save_original: true
    suffix: "_aug{index}"
```

### 預計工作量
- **實施**: 2-3 天
- **測試**: 1 天
- **文檔**: 1 天
- **總計**: 4-5 天

---

## 整合和測試

### 整合測試場景
1. **端對端工作流程**
   - 原始圖像 → Dataset Builder → Annotation Converter → Data Augmentation → 訓練就緒資料集

2. **組件互操作性**
   - Dataset Builder 輸出 → Data Augmentation 輸入
   - Annotation Converter 處理 Dataset Builder 創建的標註

3. **批量處理測試**
   - 大規模資料集處理（10,000+ 圖像）
   - 記憶體使用監控
   - 效能基準測試

### 測試資料集
- **圖像分類**: CIFAR-10, MNIST
- **物體偵測**: COCO subset, Pascal VOC
- **自訂資料集**: 動畫角色資料集

---

## 時程規劃

### Week 1 (2025-12-02 至 2025-12-08)
- **Day 1-3**: Dataset Builder 實施
- **Day 4**: Dataset Builder 測試
- **Day 5**: Dataset Builder 文檔
- **Day 6-7**: Annotation Tool Integration 實施開始

### Week 2 (2025-12-09 至 2025-12-15)
- **Day 1-2**: Annotation Tool Integration 完成
- **Day 3**: Annotation Tool Integration 測試和文檔
- **Day 4-6**: Data Augmentation Pipeline 實施
- **Day 7**: Data Augmentation 測試和文檔

### Week 3 (2025-12-16)
- **Day 1**: 整合測試
- **Day 2**: 效能優化
- **Day 3**: 更新 AUTOMATION_PROGRESS.md

---

## 技術棧

### 核心依賴
- **Pillow**: 圖像處理
- **NumPy**: 數值運算
- **scikit-image**: 進階圖像處理
- **imgaug** (optional): 資料增強
- **albumentations** (optional): 資料增強

### 標註處理
- **pycocotools**: COCO 格式處理
- **lxml**: XML 解析（Pascal VOC）
- **json**: JSON 處理

### 視覺化
- **matplotlib**: 統計圖表
- **seaborn**: 進階視覺化

---

## 成功指標

### 功能完整性
- ✅ 所有規劃功能實施完成
- ✅ 支援至少 3 種資料集格式
- ✅ 支援至少 10 種增強操作

### 品質指標
- ✅ 代碼行數: 3,000+ 行
- ✅ 測試覆蓋率: 80%+
- ✅ 文檔完整性: 3 份完整指南

### 效能指標
- ✅ 資料集創建: 1000 圖像 < 30 秒
- ✅ 格式轉換: 1000 標註 < 10 秒
- ✅ 資料增強: 100 圖像 × 5 增強 < 60 秒

### 安全指標
- ✅ CPU-only: 零 GPU 使用
- ✅ 記憶體安全: 整合 Phase 1 監控
- ✅ 錯誤處理: 完善的異常處理和回復

---

## 風險管理

### 技術風險
| 風險 | 影響 | 緩解策略 |
|------|------|----------|
| 格式轉換複雜性 | 中 | 專注於主流格式，提供完整測試 |
| 增強品質控制 | 中 | 實施品質檢查，提供預覽功能 |
| 大規模資料集處理 | 低 | 批量處理，進度追蹤，記憶體監控 |

### 時程風險
| 風險 | 影響 | 緩解策略 |
|------|------|----------|
| 功能範圍蔓延 | 中 | 嚴格遵循規劃，v2 新增功能 |
| 整合測試時間 | 低 | 單元測試先行，早期整合測試 |

---

## 文檔規劃

### 使用者文檔
1. **PHASE3_DATASET_BUILDER.md** (1000+ 行)
   - 完整功能說明
   - 命令列參考
   - 配置檔案範例
   - 工作流程範例
   - 疑難排解

2. **PHASE3_ANNOTATION_TOOLS.md** (800+ 行)
   - 格式轉換指南
   - 支援的格式詳解
   - 轉換範例
   - 驗證和修復流程

3. **PHASE3_DATA_AUGMENTATION.md** (1000+ 行)
   - 增強操作目錄
   - 管道配置指南
   - 最佳實踐
   - 視覺化範例

### 配置範例
- `dataset_builder_example.yaml`
- `annotation_converter_example.yaml`
- `data_augmentation_example.yaml`
- `phase3_batch_workflow_example.yaml`

---

## 延伸規劃（Phase 3.5）

未來可能的增強功能：
- **進階資料集分析**
  - 類別共現分析
  - 標註品質評分
  - 資料集偏差檢測

- **自動標註輔助**
  - 預訓練模型輔助標註
  - 主動學習樣本選擇
  - 標註建議和自動修正

- **資料集版本控制**
  - 資料集快照
  - 變更追蹤
  - 版本比較

---

**文檔版本**: 1.0
**最後更新**: 2025-12-02
**下次審查**: 2025-12-09 (Week 1 回顧)
