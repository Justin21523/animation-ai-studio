# SDXL ControlNet training pipeline (softedge) - template
#
# Notes:
# - Current preprocessor is canny-like + blur (fast, no extra weights).
#
# Run:
#   python scripts/processing/training/controlnet_training_pipeline.py --config configs/training/controlnet/pipelines/softedge.yaml

images_dir: null
captions_dir: null
control_images_dir: null
default_caption: ""

control_type: "softedge"
detect_resolution: 512
resolution: 1024

dataset_name: "softedge_v1"
controlnet_key: "softedge_v1"
preprocess_as: "softedge"
description: "SoftEdge ControlNet fine-tune v1"
use_case: "Soft contours for composition guidance"

controlnet_config_path: "configs/generation/controlnet_config.yaml"
trainer_config_path: "configs/training/controlnet/trainer.yaml"
registry_path: "configs/generation/controlnet_config.yaml"

