# SDXL ControlNet training pipeline (depth) - template
#
# Requirements (offline):
# - preprocessing.depth.local_path set in configs/generation/controlnet_config.yaml
#
# Run:
#   python scripts/processing/training/controlnet_training_pipeline.py --config configs/training/controlnet/pipelines/depth.yaml

images_dir: null
captions_dir: null
control_images_dir: null
default_caption: ""

control_type: "depth"
detect_resolution: 512
resolution: 1024

dataset_name: "depth_v1"
controlnet_key: "depth_v1"
preprocess_as: "depth"
description: "Depth ControlNet fine-tune v1"
use_case: "Depth-map conditioning (engine depth / consistent perspective)"

controlnet_config_path: "configs/generation/controlnet_config.yaml"
trainer_config_path: "configs/training/controlnet/trainer.yaml"
registry_path: "configs/generation/controlnet_config.yaml"

