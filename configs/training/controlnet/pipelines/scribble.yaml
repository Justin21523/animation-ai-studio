# SDXL ControlNet training pipeline (scribble) - template
#
# Notes:
# - Current preprocessor is canny-like + dilation (fast, no extra weights).
#
# Run:
#   python scripts/processing/training/controlnet_training_pipeline.py --config configs/training/controlnet/pipelines/scribble.yaml

images_dir: null
captions_dir: null
control_images_dir: null
default_caption: ""

control_type: "scribble"
detect_resolution: 512
resolution: 1024

dataset_name: "scribble_v1"
controlnet_key: "scribble_v1"
preprocess_as: "scribble"
description: "Scribble ControlNet fine-tune v1"
use_case: "Rough sketch → coherent asset generation"

controlnet_config_path: "configs/generation/controlnet_config.yaml"
trainer_config_path: "configs/training/controlnet/trainer.yaml"
registry_path: "configs/generation/controlnet_config.yaml"

